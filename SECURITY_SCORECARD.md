Generated by Merlin Studio (https://app.merlin-studio.cloud). Licensed under the Apache License, Version 2.0 (https://www.apache.org/licenses/LICENSE-2.0).

# Security Scorecard

**Score:** 100/100
**Grade:** A+

Passed: 150
Failed: 0

**Compliance Frameworks:** CIS

## Compliance Checks
These checks are performed against your configuration intent based on the selected compliance frameworks.

### Networking (1/1 passed)

| Check | Status | Severity | Frameworks | Reference | Description |
| :--- | :--- | :--- | :--- | :--- | :--- |
| delete_default_vpc | ✅ PASS | recommended | CIS | CIS 5.1 | CIS 5.1 recommends removing the default VPC and restricting default SG |

### Security Baseline (9/9 passed)

| Check | Status | Severity | Frameworks | Reference | Description |
| :--- | :--- | :--- | :--- | :--- | :--- |
| password_min_length | ✅ PASS | required | CIS | CIS 1.8 | CIS 1.8 requires minimum 14-char passwords |
| max_password_age_days | ✅ PASS | required | CIS | CIS 1.10 | CIS 1.10 requires password rotation within 90 days |
| password_reuse_prevention | ✅ PASS | required | CIS | CIS 1.9 | CIS 1.9 requires preventing reuse of last 24 passwords |
| cloudtrail_org_enabled | ✅ PASS | required | CIS | CIS 3.1 | CIS 3.1 requires CloudTrail enabled in all regions |
| cloudtrail_multi_region | ✅ PASS | required | CIS | CIS 3.1 | CIS 3.1 requires multi-region trail |
| cmek_log_encryption | ✅ PASS | required | CIS | CIS 3.7 | CIS 3.7 requires CloudTrail logs encrypted with CMK |
| aws_config_enabled | ✅ PASS | required | CIS | CIS 3.5 | CIS 3.5 requires AWS Config enabled in all regions |
| guardduty_enabled | ✅ PASS | required | CIS | CIS 4.16 | CIS 4.16 requires GuardDuty enabled |
| security_hub_enabled | ✅ PASS | recommended | CIS | CIS-extended | CIS recommends Security Hub for aggregated findings |

### Compute Engine (2/2 passed)

| Check | Status | Severity | Frameworks | Reference | Description |
| :--- | :--- | :--- | :--- | :--- | :--- |
| ebs_default_encryption | ✅ PASS | required | CIS | CIS 2.2.1 | CIS 2.2.1 — Ensure EBS volume encryption is enabled in all regions |
| imdsv2_required | ✅ PASS | required | CIS | CIS 5.4 | CIS 5.4 — Require IMDSv2 on all EC2 instances (defends against SSRF token theft) |

## Technical Infrastructure Checks (Checkov)
**Passed:** 138 &nbsp;&nbsp; **Failed:** 0

> File paths below reference the canonical **Terraform** (`.tf`) representation that Checkov scans. If your bundle ships a different output format (e.g. tfvars, LZA YAML, or CDK), the `.tf` files are not included — apply the fix to the field of the **same name** in your format's equivalent file, or change it in the wizard and re-generate.

## Suppressed Checks

These Checkov findings are suppressed intentionally — the associated risk is mitigated by the architecture rather than the flagged attribute. The rationale for each is shown below (and inline in the OpenTofu `.tf` source).

| Check | Resource | Rationale |
|-------|----------|-----------|
| `CKV_AWS_274` | `aws_ssoadmin_managed_policy_attachment.admin_aws_managed` — `iam.tf`:L39 | Identity Center cloud-admin / break-glass / CI-apply role; defense-in-depth is org SCPs + CloudTrail alerting, not a boundary on the role itself. |
| `CKV_AWS_274` | `aws_iam_role_policy_attachment.breakglassrole_administratoraccess` — `iam.tf`:L92 | Identity Center cloud-admin / break-glass / CI-apply role; defense-in-depth is org SCPs + CloudTrail alerting, not a boundary on the role itself. |
| `CKV_AWS_274` | `aws_iam_role_policy_attachment.wl_terraformexecutor_administratoraccess` — `iam.tf`:L130 | Identity Center cloud-admin / break-glass / CI-apply role; defense-in-depth is org SCPs + CloudTrail alerting, not a boundary on the role itself. |
| `CKV_AWS_109` | `aws_iam_policy_document.kms_default` — `kms.tf`:L31 | KMS key policy grants the account root kms:* (AWS-prescribed EnableRoot pattern, required for the key to stay manageable); principal scoping bounds the blast radius. |
| `CKV_AWS_111` | `aws_iam_policy_document.kms_default` — `kms.tf`:L31 | Same as CKV_AWS_109 — kms:Update*/Schedule* on the root principal is the documented KMS baseline. |
| `CKV_AWS_356` | `aws_iam_policy_document.kms_default` — `kms.tf`:L31 | Resource="*" is required at the KMS key-policy layer; principal scoping bounds the blast radius. |
| `CKV_TF_1` | `vpc_main` — `vpc.tf`:L33 | Module is sourced by a version-pinned registry reference, not a mutable git ref. |
| `CKV_AWS_18` | `aws_s3_bucket.central_logs` — `s3_log_archive.tf`:L30 | Access logging on the log-archive bucket is circular; CloudTrail S3 data events + SNS notifications cover this surface. |
| `CKV_AWS_144` | `aws_s3_bucket.central_logs` — `s3_log_archive.tf`:L30 | Cross-region replication is opt-in — enable it in the wizard (replication_enabled + destination region) for a full CRR pipeline. Single-region keeps the audit archive in one data-residency boundary, with versioning + Object Lock (WORM) protecting it. |

