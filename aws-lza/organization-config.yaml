# Generated by Merlin Studio (https://app.merlin-studio.cloud). Licensed under the Apache License, Version 2.0 (https://www.apache.org/licenses/LICENSE-2.0).

# -----------------------------------------------------------------------------
# Generated by Merlin Studio v2 (architex-v2) — LZA organization-config.yaml
# Organization: AcmeCorp
# Profile:      SIMPLE
# Generated:    2026-06-17T09:00:13.913147Z
# Wizard:       v1.0.0
# -----------------------------------------------------------------------------
# Scaffolding — not a finished artifact. Merlin renders this from your
# declared intent (profile, frameworks, accounts, topology). Adapt it to
# your environment: tighten CIDRs, refine policy bodies, add team tags,
# pin module versions, layer account-specific overrides. Re-run the wizard
# when *intent* shifts; keep day-to-day customisation in your IaC fork
# (regen overwrites this directory).
# Replace PLACEHOLDER_ tokens with real values from your AWS Organization
# before `tofu apply` / `cdk deploy` / LZA pipeline run — see PLACEHOLDERS.md.
# Compliance frameworks declared on this spec:
#   - cis

# AWS Organizations structure, OU tree, and Service Control Policies.
# Schema: https://awslabs.github.io/landing-zone-accelerator-on-aws/v1.14/typedocs/

enable: true

# OU tree — source of truth: generators/aws_ou_tree.py. Covers the profile
# baseline, custom OUs, and every OU an account is assigned to (incl. nested
# paths) so accounts-config.yaml never references a missing OU. Suspended is
# always present so the Quarantine SCP has a non-empty deploymentTargets list.
organizationalUnits:
  - name: Security
  - name: Workloads
  - name: Suspended

quarantineNewAccounts:
  enable: true
  scpPolicyName: Quarantine-Policy

# SCP set sourced from generators/aws_scp_policies.py — identical across
# OpenTofu / LZA / CDK / tfvars (Quarantine + Guardrails-Security/Sandbox +
# region restrictions under FedRAMP/GDPR). The matching policy JSON files are
# emitted by the LZA composer.
serviceControlPolicies:
  - name: Quarantine-Policy
    description: Deny all actions for newly created accounts until reviewed.
    policy: service-control-policies/quarantine.json
    type: customerManaged
    deploymentTargets:
      organizationalUnits:
        - Suspended
  - name: Require-IMDSv2
    description: "Deny launching EC2 instances without IMDSv2 (http_tokens=required) and deny weakening the metadata options afterwards. Prevention paired with the ec2-imdsv2-check Config rule's detection. CIS 5.4/5.6; NIST/FedRAMP AC-3 + SC-23; PCI-DSS Req 2; HIPAA technical safeguards."
    policy: service-control-policies/require-imdsv2.json
    type: customerManaged
    strategy: deny-list
    deploymentTargets:
      organizationalUnits:
        - Root

taxonomy:
  enable: true

backupPolicies:
  - name: Org-Backup-Policy-Daily
    description: Daily backup of in-scope resources to a central vault.
    policy: backup-policies/org-backup-daily.json
    deploymentTargets:
      organizationalUnits:
        - Workloads

tagPolicies:
  - name: Standard-Tagging
    description: Enforce standard tag keys (CostCenter, Owner, Environment) on supported resources.
    policy: tag-policies/standard-tagging.json
    deploymentTargets:
      organizationalUnits:
        - Root